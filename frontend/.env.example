# Base URL of the ASP.NET Core API the SPA talks to.
# Copy this file to .env.local to override locally.
# If unset, the app defaults to http://localhost:5270 (see src/api/client.ts).
VITE_API_BASE_URL=http://localhost:5270
